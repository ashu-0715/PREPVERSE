type ModuleTheme = "career" | "notes" | "skillswap" | "gamezone" | "exam" | "tracker";

const ModuleBackground = ({ theme: _theme }: { theme: ModuleTheme }) => null;

export default ModuleBackground;
